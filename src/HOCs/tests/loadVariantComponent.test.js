import React from 'react';
import { render } from '@testing-library/react';
import loadVariantComponent from "../loadVariantComponent";

describe('loadVariantComponent', function () {
    //TODO
    test('it returns the correct component for the particular variant', () => {
        //mockVariant('A')
    })
});
